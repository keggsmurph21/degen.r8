const size = 1000;

// viewBox coords
const x = -size / 2;
const y = -size / 2;
const width = size;
const height = size
export const viewBox = `${x} ${y} ${width} ${height}`;

export const tableRadius = size * 1 / 4;
export const cardWidth = tableRadius / 4;
export const cardHeight = cardWidth * 4 / 3;
export const cardPaddingX = cardWidth / 8;

